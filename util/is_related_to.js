const isRelatedTo = (userId, currentUser) => {
  if (userId === currentUser.id) { return true }
  // for (let i = 0; i < currentUser.relationships.length(); i++) {
  //   for (let j = 0; j < relationships[i].length(); j++) {

  //   }
  // }
}

module.exports = isRelatedTo;